#include "boxstructure.h"
#include "binpack.h"
#include <iostream>
#include <vector>
using namespace std;

int main(int argc, char* argsv[])
{
    for (int i = 0; i < argc; i++)
    {
        cout << argsv[i];
    }
    return 0;
}